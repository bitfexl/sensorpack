import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
    appId: "com.github.bitfexl.sensorpack",
    appName: "sensorpack",
    webDir: "dist",
};

export default config;
